<template>
	<v-dialog
		:model-value="opened"
		:max-width="maxWidth ? maxWidth : 800"
		:fullscreen="$vuetify.display.mobile"
		>
			<v-card :loading="loading" class="dark-glass">
				<v-toolbar :title="title">
					<template #append>
						<CloseBtn @close="$emit('close')" />
					</template>
				</v-toolbar>
				<v-card-text>
					<slot name="text"></slot>
				</v-card-text>
				<v-card-actions>
					<slot name="actions"></slot>
				</v-card-actions>
			</v-card>
	</v-dialog>
</template>

<script lang="ts" setup>
	import CloseBtn from "../Containment/Btns/CloseBtn.vue";
	const emit = defineEmits(["close"]);
	defineProps<{
		maxWidth?: number;
		opened: boolean;
		title: string;
		loading: boolean;
	}>();
</script>

<style></style>
